# 2026-03-26

## Kiro Quant / Kiro Quant V3（kiro-quant-v3）

- **用戶資金分倉偏好（建倉資金池）**：
  - 50% 長期（long）
  - 30% 中期（mid）
  - 10% 短期（short）
  - 10% 備用現金（reserve，永遠保留唔用）

- **用 MA200 / MA80 / MA30 分 bucket（機械化規則）**：
  - long：現價 ≥ MA200
  - mid：MA80 ≤ 現價 < MA200
  - short：MA30 ≤ 現價 < MA80
  - avoid：現價 < MA30（avoid bucket = 0%，唔開新倉）
  - 當晚以最新日線計嘅初步結果（供參考）：
    - long：NVDA, AAPL, GOOGL, AMD, URA
    - short：AMZN, PLTR
    - avoid：TSLA, MSFT, META, F, OKLO, TSLL

- **用戶選擇「B」模式（分析偏好影響短線賣出）**：
  - 在 `kiro-quant-v3/v3_pipeline/core/main_loop.py` 加入 **SELL confirmation streak**：
    - sentiment_score ≥ 0.10：需要連續 2 次 model SELL signal 才 SELL
    - sentiment_score ≥ 0.50：需要連續 3 次 model SELL signal 才 SELL
  - 止損/止盈/追蹤止損仍然即時生效（唔延遲風控）。

- **NO_FUTU=1（YF-only）狀態一致性修正**：
  - 曾出現 NVDA 重覆 SELL（紙上交易）原因：NO_FUTU 依賴 `state.json` 同步持倉，state 未更新會令每個 cycle 當仲有倉。
  - 已在 `_execute()` 加入：每次 BUY/SELL 都會寫入 `trades.jsonl`，並同步更新 `state.json`（positions + last_orders + daily_trades），避免重啟/同步後重覆落單。
  - 手動修正過 `kiro-quant-v3/state.json`：US.NVDA → 0（反映已 SELL）。

- **情緒引擎接入 V3**：
  - V3 loop 每 cycle 會讀 `kiro-quant-v3/sentiment.json`（score + summary），並將 sentiment 影響策略 profile / allow_long / risk multiplier（StrategyFactory）。

## Cron Jobs（Telegram: 625655860）

- 曾出現「US market / Trading Agent」cron job 不在 scheduler（用戶反映無執行）。已重新建立：
  - `US Market - Warm-up & Supervision (21:15 HKT)`（jobId: **beeb7026-4361-478c-a8b3-fca20179c3e1**）
  - `US Market - Trading Agent Sentiment Pulse (30m)`（jobId: **1693c222-ba02-48a8-9cf1-5f38aa836b11**）

- `Kiro Quant 美股巡航狀態` cron job 之前頻繁 timeout；已更新 payload：
  - timeoutSeconds 提升到 600
  - 內容改為「以本地狀態為主、避免長篇 web_search」
  - jobId: **6225a241-8762-4b06-95c0-1e90a9349469**
