## Kiro Quant durable state
- Canonical version set to `v3.6-unified-20260318` in `kiro-quant/VERSION.md`; source of truth runtime is `kiro-quant/v3_launcher.py` + `kiro-quant/v3_pipeline/`.
- Unified baseline includes market auto-switch by Hong Kong time: `09:00-16:00 -> HK`, `21:30-04:00 -> US`, otherwise `IDLE` collect-only; market switch logs like `[MARKET_SWITCH: HK→US]`.
- Strategy guard tightening merged into unified version: `buy_cooldown_cycles=4`, `swing_buy_confirmation_count=5`, `swing_block_on_model_sell=true`, `quick_take_profit_pct=0.02`, `stop_loss_pct=0.02`, `max_hold_bars=5`.
- Swing entry logic was tightened to `near_support AND (RSI oversold OR MACD cross up)`; swing buys are blocked when `model_sell=True` via `SWING_MODEL_FILTER` logging.
- `v3_launcher.py` runtime profiles were aligned from `input_dim=26` to `input_dim=25` after confirming both HK and US real feature schemas produce 25 features; `python3 v3_launcher.py --dry-run` and `--dry-run --profile standard` both passed after the fix.
- Active runner was restarted onto the unified version on 2026-03-18 night; latest observed process was `python3 v3_launcher.py --profile lite` with PID `1946398`, log `kiro-quant/logs/openclaw_v3_lite_20260318_234557.log`.
- Futu/OpenD was not online during this session; current runs used `PAPER_YFINANCE` / YFinance fallback despite connector improvements.

## User preference
- User wants future strategy review / strategy-fix reports routed to the Telegram group/topic for `tsukiiaiplatfrom`, ideally a topic named `策略修正報告`, once the bot has routing/topic context from a message inside that topic.
